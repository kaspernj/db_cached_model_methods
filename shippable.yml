language: ruby
cache: bundler
archive: true
rvm:
  - ruby-2.1.2
script:
  - cd spec/dummy
  - RAILS_ENV=test bundle exec rake db:migrate
  - cd ../..
  - bundle exec rspec
  - bundle exec rubocop --rails
notifications:
   email: false
